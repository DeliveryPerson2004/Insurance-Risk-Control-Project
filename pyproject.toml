[project]
name = "insurance-fraud-detection"
version = "0.4.0"
description = "医疗保险理赔欺诈检测系统"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "xgboost>=2.0",
    "scikit-learn>=1.3",
    "optuna>=3.0",
    "shap>=0.44",
    "matplotlib>=3.7",
    "seaborn>=0.12",
    "joblib>=1.3",
    "openpyxl>=3.1",
    "imbalanced-learn>=0.11",
]

[tool.uv]
# 脚本项目，不需要构建为 Python 包
